Disruptor - 分裂，瓦解
1.一个金融公司的开源框架
2.是单机最快的MQ. 性能极高，无锁 CAS，单机支持高并发。
3.在内存里，磁盘上存不了
4.内存里的存放高效元素的队列。


5.不是Redis，Kafa 是可以有和它们一样的用途
但Redis可以有序列化机制，可以存在磁盘上

6.无锁，高并发，使用环形Buffer，直接覆盖（不用清除）旧的数据，降低GC频率

  实现了基于事件的生产者消费者模式（观察者模式：一直看着队列有没有新东西，有就消费掉）

链表遍历一定比数组慢，同时要维护头指针和尾指针。但是循环数组只要维护一个 位置（Sequence ： 下一下有效位置在哪）
只有一个指针来回转。位置覆盖（覆盖时有策略的：消费者拿走了队列中的东西，所有覆盖之后就找不到了，除非在拿走之前被记录一下），
环形数组里存的是一个个的事件（Event e）由Event 工厂产生，要指定消费者。

7.



